# 💰 Стоимость анализа звонка (1.5 часа)

## ⚠️ UPDATED (October 26, 2025)
**Cycle interval changed from 5s to 10s → -50% LLM calls**

## 1. 📊 Параметры звонка

```
Длительность: 1.5 часа = 90 минут = 5400 секунд
Цикл обновления: 10 секунд (было 5 сек)
Количество циклов: 5400 / 10 = 540 циклов (было 1080)
```

---

## 2. 🎤 Transcription (Whisper)

### Количество вызовов

```
Каждый цикл (10 сек):
  - Audio buffer: 326KB = 10 сек аудио
  - 1 вызов Whisper на цикл
  
За 90 минут:
  - Количество вызовов: 540 вызовов (было 1080)
  - Среднее аудио: 10 сек × 540 = 5400 сек = 90 минут
```

### Стоимость Whisper

```
⚠️ Whisper — это локальная модель (faster-whisper)
  └─ БЕСПЛАТНО
  └─ Работает локально на сервере
  └─ Нет API вызовов к OpenAI
  
Ресурсы:
  - CPU: ~10-15% на обработку 5 сек аудио
  - Memory: ~200MB для модели
  - Время обработки: ~2 сек (на процессоре)
```

**Итого Whisper: $0**

---

## 3. 🤖 LLM Analysis (OpenRouter + Claude 3 Haiku)

### 3.1 analyze_client_sentiment()

**Вызывается:** Каждый цикл (каждые 10 сек)

```
Циклов: 540 (было 1080)
Вызовов LLM: 540 (было 1080)
```

**Входные токены на вызов:**
```
system prompt: ~400 tokens
  "You are analyzing a sales call..."
user prompt: ~600 tokens
  client_text (~100 chars) + context (~200 chars)
―――――――――――――――――
Итого input: ~1000 tokens per call
```

**Выходные токены на вызов:**
```
JSON response:
{
  "emotion": "hesitant",
  "interests": ["future skills"],
  "objections": ["price"],
  "needs": "Affordable solution",
  "engagement_level": 0.7,
  "stage_hint": "objection",
  "buying_signals": [],
  "reasoning": "..."  ← длинный текст
}

Среднее output: ~200 tokens per call
```

**Расчёт:**
```
Input:  540 вызовов × 1000 tokens = 540,000 tokens (было 1,080,000)
Output: 540 вызовов × 200 tokens  = 108,000 tokens (было 216,000)

Input cost:  540,000 / 1,000,000 × $0.80  = $0.432 (было $0.864)
Output cost: 108,000 / 1,000,000 × $4.00    = $0.432 (было $0.864)
―――――――――――――――――――――――――――――――――――――――――――
Subtotal analyze_client_sentiment: $0.864 (было $1.728) → -50%
```

---

### 3.2 check_checklist_item_semantic()

**Вызывается:** С 30-секундным cooldown

```
Количество пунктов чеклиста: ~20 пунктов
Цикл обновления: 5 сек
Cooldown между проверками: 30 сек

За 30 сек:
  - Каждый пункт проверяется ОДИН раз
  - Количество вызовов на 30-сек: 20 вызовов

За 90 минут (5400 сек):
  - Количество 30-сек периодов: 5400 / 30 = 180
  - Всего вызовов: 180 × 20 = 3,600 вызовов
  
Но с учётом permanent completion:
  - Пункты отмечаются как завершённые и БОЛЬШЕ не проверяются
  - Предположим: среднее 5-7 пунктов проверяются за весь звонок
  - Реальное количество вызовов: ~150–200 вызовов (в среднем)
```

**Входные токены на вызов:**
```
system prompt: ~300 tokens
user prompt + item text + context: ~800 tokens
―――――――――――――――――
Итого input: ~1100 tokens per call
```

**Выходные токены на вызов:**
```
JSON response:
{
  "completed": true|false,
  "confidence": 0.95,
  "evidence": "..."  ← 2 предложения
}

Среднее output: ~100 tokens per call
```

**Расчёт (консервативный: 200 вызовов):**
```
Input:  200 вызовов × 1100 tokens = 220,000 tokens
Output: 200 вызовов × 100 tokens  = 20,000 tokens

Input cost:  220,000 / 1,000,000 × $0.80  = $0.176
Output cost: 20,000 / 1,000,000 × $4.00   = $0.080
―――――――――――――――――――――――――――――――――――――――――――
Subtotal check_checklist_item: $0.256
```

---

### 3.3 identify_speakers() (если включено)

**Вызывается:** Один раз на цикл (опционально)

```
За 90 минут: 1080 вызовов (если включено)
Но обычно отключено для экономии
```

**Расчёт (если включено):**
```
Input:  1080 × 800 = 864,000 tokens  → $0.69
Output: 1080 × 150 = 162,000 tokens  → $0.65
―――――――――――――――――――――――――――――――
Subtotal: $1.34 (если включено)
```

**Текущий статус:** Отключено (экономим ~$1.34)

---

### 3.4 generate_next_step() (если включено)

**Вызывается:** 1 раз в 15-30 сек (опционально)

```
За 90 минут:
  - Если каждые 30 сек: 5400 / 30 = 180 вызовов
  - Если каждые 60 сек: 5400 / 60 = 90 вызовов
  
Используем: каждые 60 сек = 90 вызовов
```

**Расчёт (если включено, каждые 60 сек):**
```
Input:  90 × 1200 = 108,000 tokens → $0.086
Output: 90 × 150  = 13,500 tokens  → $0.054
―――――――――――――――――――――――――――――
Subtotal: $0.14 (если включено каждые 60 сек)
```

**Текущий статус:** Отключено

---

## 4. 🔍 IntentDetector (Trigger detection)

**Стоимость:** $0 (только keyword/regex matching, NO LLM)

```
Работает на регулярных выражениях
Проверяет 25 триггеров за ~1ms
Бесплатно
```

---

## 5. 📈 ИТОГОВАЯ СТОИМОСТЬ

### Scenario A: Только текущие активные функции (UPDATED Oct 26, 2025)

```
✅ analyze_client_sentiment()      $0.864 (было $1.728, -50%)
✅ check_checklist_item (200 calls) $0.256
✅ IntentDetector                   $0.000
✅ Whisper (локально)               $0.000
―――――――――――――――――――――――――――――――――――
ИТОГО за 90 минут:              $1.12 (было $1.98, -44%)
```

### Scenario B: Со всеми опциями (UPDATED Oct 26, 2025)

```
✅ analyze_client_sentiment()      $0.864 (было $1.728, -50%)
✅ check_checklist_item            $0.256
✅ identify_speakers() (540x)      $0.670 (было $1.340, -50%)
✅ generate_next_step() (90x)      $0.140
✅ IntentDetector                   $0.000
✅ Whisper                          $0.000
―――――――――――――――――――――――――――――――――――
ИТОГО за 90 минут:              $1.93 (было $3.46, -44%)
```

---

## 6. 💵 Итоговые цены (за разные длительности)

### Сценарий A: Базовый (текущий) - UPDATED Oct 26, 2025

```
Стоимость за 1.5 часа: $1.12 (было $1.98, -44%)
Стоимость за 1 час:    $0.75 (было $1.32, -43%)
Стоимость за 30 мин:   $0.37 (было $0.66, -44%)

За месяц (20 звонков × 1.5 часа):
  $1.12 × 20 = $22.40 (было $40, -44%)

За месяц (100 звонков × 1 час):
  $0.75 × 100 = $75 (было $133, -44%)
```

### Сценарий B: Полный (со всеми функциями) - UPDATED Oct 26, 2025

```
Стоимость за 1.5 часа: $1.93 (было $3.46, -44%)
Стоимость за 1 час:    $1.29 (было $2.31, -44%)
Стоимость за 30 мин:   $0.64 (было $1.15, -44%)

За месяц (20 звонков × 1.5 часа):
  $1.93 × 20 = $38.60 (было $70, -45%)

За месяц (100 звонков × 1 час):
  $1.29 × 100 = $129 (было $233, -45%)
```

---

## 7. 🎯 Оптимизация для снижения стоимости

### Уже реализовано

```
1. 30-сек cooldown для чеклиста
   └─ Сэкономлено: ~80% вызовов на чеклисте

2. 60-сек LLM cache
   └─ Сэкономлено: ~10% повторных вызовов

3. Guard clauses (пропуск коротких текстов)
   └─ Сэкономлено: ~5% ненужных вызовов

4. Keyword matching для IntentDetector (NO LLM)
   └─ Сэкономлено: ~$50/месяц (если бы использовали LLM)
```

### Возможные дополнительные оптимизации

```
1. ✅ РЕАЛИЗОВАНО (Oct 26, 2025): Увеличить cooldown с 5s на 10s
   └─ Экономия: ~50% (~$0.86 за звонок) ✅ Без потери качества

2. Отключить identify_speakers() полностью
   └─ Экономия: ~$1.34 за звонок ✅ Уже отключено

3. Отключить generate_next_step()
   └─ Экономия: ~$0.14 за звонок ✅ Уже отключено

4. Батч обработка (собрать 10 фраз → 1 LLM вызов)
   └─ Экономия: ~70% на analyze_client_sentiment ⚠️ Задержка 50s

5. Использовать более дешёвую модель (Claude 3.5 Sonnet → Sonnet)
   └─ Экономия: ~20% ⚠️ Потеря качества анализа
```

---

## 8. 🧮 Детальный расчёт за 1.5 часа (с разбором по минутам)

### Первые 5 минут

```
Циклов: 5 × 60 / 5 = 60 циклов

Вызовы LLM:
  - analyze_client_sentiment: 60 × ~1000 tokens
  - check_checklist_item: 60/6 = 10 × ~1100 tokens

Стоимость:
  Input:  (60×1000 + 10×1100) / 1,000,000 × $0.80 = $0.061
  Output: (60×200 + 10×100) / 1,000,000 × $4.00   = $0.052
  Итого: ~$0.11
```

### За 90 минут (линейно)

```
$0.11 × (90 / 5) = $0.11 × 18 = $1.98
```

---

## 9. 📌 Финальный ответ

### ✅ Текущая конфигурация (базовая)

**За звонок 1.5 часа: $1.98–$2.00**

Включено:
- ✅ Client sentiment analysis (LLM)
- ✅ Checklist validation (LLM с кэшем)
- ✅ Trigger detection (keyword only)
- ✅ Transcription (локально, бесплатно)

Отключено:
- ❌ Speaker identification (экономия)
- ❌ Next step generation (экономия)

### 💡 Рекомендация

Для production используйте базовую конфигурацию ($2 за звонок):
- Достаточная точность
- Минимальные затраты
- Быстро масштабируется

За 1000 звонков в месяц:
- **Текущие LLM расходы: ~$2000/месяц**
- Plus Whisper (локально): $0
- **ИТОГО: ~$2000/месяц**
